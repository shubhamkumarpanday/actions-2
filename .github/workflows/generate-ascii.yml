name: generate_ascii
on: [push]

jobs:
  ascii_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        run: actions/checkout@v4

      - name: executing shell script
        run: ./ascii-script.sh

      - name: list repo files
        run: ls -ltra
